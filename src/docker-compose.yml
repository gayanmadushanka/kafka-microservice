version: "3.8"

services:
  services.order:
    image: services.order
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5003:5003"
    networks:
      - product-network

networks:
  product-network:
    driver: bridge
